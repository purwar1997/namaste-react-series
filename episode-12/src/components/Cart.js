import useFetchRestaurantData from '../utils/useFetchRestaurantData';

const Cart = ({ cartItems }) => {
  //  const restaurantInfo = useFetchRestaurantData();

  console.log(cartItems);

  return <div>cart</div>;
};

export default Cart;
